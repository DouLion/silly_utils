message("\n检查库 LibArchive")
if(DISABLE_LIBARCHIVE)
    message(STATUS "禁用LIBARCHIVE")
else()
    find_package(LibArchive REQUIRED)
    if(LibArchive_FOUND)
        message(STATUS "LibArchive found: ${LibArchive_FOUND}")
        message(STATUS "LibArchive version: ${LibArchive_VERSION}")
        list(APPEND SU_ALL_REQUIRED_LIBRARIES LibArchive::LibArchive)
        set(SU_THIRD_SUPPORT_LIBARCHIVE 1)
    else()
        message(WARNING "未发现LIBARCHIVE")
    endif()
endif()