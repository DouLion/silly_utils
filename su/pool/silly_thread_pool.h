//
// Created by root on 2024/4/26.
//

/*#ifndef SILLY_UTILS_SILLY_THREAD_POOL_H
#define SILLY_UTILS_SILLY_THREAD_POOL_H

// 线程池
class silly_thread_pool {
    silly_thread_pool();
    silly_thread_pool(size_t num);
    template <typename Func, typename Obj, typename... Args>
    void add(Func&& func, Obj&& obj, Args&&... args);

    template <typename Func, typename Obj, typename... Args>
    void add(Func&& func, Args&&... args);

    void start();

private:
    size_t m_num{4};
};


#endif //SILLY_UTILS_SILLY_THREAD_POOL_H*/
